import React, { Component } from 'react'
import { ButtonsContainer } from '../poems/styles'
import Button from '../button'


function ControlButtons(props) {

//  return (
//      <ButtonsContainer>
//        <Button button disabled={!props.hasPrevious} onClick={props.handlePrevPrev} text="&laquo;"/>
//        <Button button disabled={!props.hasPrevious} onClick={props.handlePrev} text='&lsaquo;'/>
//        <Button button disabled={false} onClick={handleRand} text={props.currentPage} />
//        <Button button disabled={!props.hasNext} onClick={props.handleNext} text='&rsaquo;'/>
//        <Button button disabled={!props.hasNext} onClick={props.handleNextNext} text='&raquo;'/>
//      </ButtonsContainer>
//
//  )
}

export default ControlButtons
